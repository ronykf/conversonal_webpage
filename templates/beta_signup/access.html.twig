<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>Beta Signup Access - Conversonal</title>
		<link rel="icon" href="{{ asset('images/conversonal_icon.png') }}" type="image/x-icon">
		<link rel="preconnect" href="https://fonts.googleapis.com">
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
		<link href="https://fonts.googleapis.com/css2?family=Lexend:wght@300;400;500;600;700&display=swap" rel="stylesheet">
		<link rel="stylesheet" href="{{ asset('css/landing.css') }}">
		<style>
			.access-container {
				min-height: 100vh;
				display: flex;
				align-items: center;
				justify-content: center;
				background: linear-gradient(180deg, #FFFFFF 0%, #F9FAFB 100%);
				padding: 24px;
			}

			.access-box {
				width: 100%;
				max-width: 480px;
				background: #FFFFFF;
				border-radius: 24px;
				box-shadow: 0px 25px 50px -12px rgba(0, 0, 0, 0.1);
				padding: 64px 48px;
				text-align: center;
			}

			.access-logo {
				margin-bottom: 32px;
			}

			.access-heading {
				font-family: 'Lexend', sans-serif;
				font-weight: 500;
				font-size: 32px;
				line-height: 40px;
				letter-spacing: -0.8px;
				color: #101828;
				margin-bottom: 12px;
			}

			.access-paragraph {
				font-family: 'Lexend', sans-serif;
				font-weight: 400;
				font-size: 16px;
				line-height: 24px;
				color: #4A5565;
				margin-bottom: 40px;
			}

			.access-form {
				display: flex;
				flex-direction: column;
				gap: 24px;
			}

			.access-input {
				font-family: 'Lexend', sans-serif;
				font-weight: 400;
				font-size: 16px;
				line-height: 24px;
				color: #101828;
				background: #FFFFFF;
				border: 1px solid #D1D5DB;
				border-radius: 8px;
				padding: 12px 16px;
				outline: none;
				transition: all 0.2s ease;
			}

			.access-input:focus {
				border-color: #800000;
				box-shadow: 0 0 0 3px rgba(128, 0, 0, 0.1);
			}

			.access-submit {
				background: #800000;
				border-radius: 10px;
				padding: 16px 32px;
				border: none;
				font-family: 'Lexend', sans-serif;
				font-weight: 500;
				font-size: 16px;
				line-height: 24px;
				color: #FFFFFF;
				cursor: pointer;
				transition: all 0.2s ease;
			}

			.access-submit:hover {
				background: #660000;
				box-shadow: 0px 10px 15px -3px rgba(128, 0, 0, 0.2);
			}

			.access-error {
				background: #FEE2E2;
				border: 1px solid #FCA5A5;
				border-radius: 8px;
				padding: 12px 16px;
				font-family: 'Lexend', sans-serif;
				font-size: 14px;
				color: #991B1B;
				margin-bottom: 16px;
			}

			.back-link {
				display: inline-block;
				margin-top: 24px;
				font-family: 'Lexend', sans-serif;
				font-size: 14px;
				color: #800000;
				text-decoration: none;
				transition: color 0.2s ease;
			}

			.back-link:hover {
				color: #660000;
				text-decoration: underline;
			}
		</style>
	</head>
	<body>
		<div class="access-container">
			<div class="access-box">
				<div class="access-logo">
					<svg width="48" height="48" viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg">
						<rect width="48" height="48" rx="12" fill="#800000"/>
						<path d="M24 14V34M14 24H34" stroke="white" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"/>
					</svg>
				</div>

				<h1 class="access-heading">Beta Signup Access</h1>
				<p class="access-paragraph">Enter the access code to view the beta signup list</p>

				{% if error %}
					<div class="access-error">
						{{ error }}
					</div>
				{% endif %}

				<form class="access-form" method="POST">
					<input
						type="password"
						name="access_code"
						class="access-input"
						placeholder="Enter access code"
						required
						autocomplete="off"
					>
					<button type="submit" class="access-submit">
						Access List
					</button>
				</form>

				<a href="{{ path('landing_page') }}" class="back-link">← Back to Home</a>
			</div>
		</div>
	</body>
</html>
